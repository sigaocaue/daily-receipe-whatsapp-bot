# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

A Python bot that sends daily recipe messages via WhatsApp.

## Getting Started

This project is in its initial state. As the codebase is built out, update this file with:

- How to install dependencies (e.g., `pip install -r requirements.txt` or `uv sync`)
- How to run the bot (e.g., `python main.py`)
- How to run tests (e.g., `pytest` or `pytest tests/test_foo.py::test_bar`)
- Required environment variables (API keys, phone numbers, scheduling config)
